import React from 'react';

// My Components

// TODO: ここで画面の管理をする？
// とりあえず一画面ずつ作る
function MenuListView() {
    const dummyListData = [...Array(60).keys()];
    const ListComponent = dummyListData.map((e) => {

    });

    return (
        <span>TEST</span>
    );
}

export default MenuListView;
